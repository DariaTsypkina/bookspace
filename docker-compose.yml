services:
  postgres:
    image: postgres:16-alpine
    container_name: bookspace-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: bookspace
      POSTGRES_PASSWORD: bookspace
      POSTGRES_DB: bookspace
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bookspace -d bookspace"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: bookspace-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pg_data:
  redis_data:
